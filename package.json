{
  "name": "token-shrinker-workspace",
  "version": "0.2.0",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "typescript": "7.0.2"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm --filter @token-shrinker/sdk build && pnpm --filter \"./packages/adapter-*\" build && pnpm -r --if-present lint",
    "typecheck": "pnpm --filter @token-shrinker/sdk build && pnpm --filter \"./packages/adapter-*\" build && pnpm -r --if-present typecheck",
    "check:fixture": "node scripts/verify-demo-fixture.mjs",
    "check:contracts": "node scripts/verify-m0-contracts.mjs",
    "check:reference": "node scripts/generate-reference.mjs --check",
    "check:protocol": "node scripts/generate-protocol.mjs --check",
    "check:adapters": "node scripts/verify-adapters.mjs",
    "check:claude-plugin": "node scripts/verify-claude-plugin.mjs",
    "check:codex-plugin": "node scripts/verify-codex-plugin.mjs",
    "check:rust": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo test --workspace",
    "check:native": "node scripts/verify-native-package.mjs",
    "check:release": "node scripts/verify-release-metadata.mjs",
    "package:native": "node scripts/package-native.mjs",
    "check:secrets": "node scripts/scan-secrets.mjs",
    "check:licenses": "node scripts/check-node-licenses.mjs",
    "check:demo": "cargo run -q -p token-shrinker-cli -- benchmark demo --json",
    "check:m7": "pnpm --filter ./packages/vscode package && node scripts/verify-m7.mjs",
    "check": "pnpm check:rust && pnpm check:fixture && pnpm check:contracts && pnpm check:reference && pnpm check:protocol && pnpm check:secrets && pnpm check:licenses && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm check:adapters && pnpm check:claude-plugin && pnpm check:codex-plugin && pnpm check:native && pnpm check:demo && pnpm check:m7 && pnpm check:release"
  }
}
